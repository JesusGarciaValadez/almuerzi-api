{
  "name": "demo-gatitos",
  "version": "0.0.1",
  "description": "Demo de sitio de gatitos",
  "keywords": "demo, gatitos, kittens",
  "license": "MIT",
  "author": "JesusGarciaValadez",
  "scripts": {
    "build": "parcel build public/index.html",
    "dev": "parcel public/index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "now": "^19.0.1",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.18"
  }
}
